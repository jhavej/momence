{
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsc && dotenv -e ../.env node dist/index.js",
    "vercel-build": "tsc && find dist -name '*.js' -exec sh -c 'mv \"$1\" \"${1%.js}.cjs\"' _ {} \\;"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "dotenv": "^16.3.1",
    "typescript": "^5.3.3"
  }
}
